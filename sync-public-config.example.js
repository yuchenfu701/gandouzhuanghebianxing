/**
 * 复制本文件为 sync-public-config.js 后填写下面两项（与管理员「云同步」里一致），
 * 或在 sync-public-config.js 中直接修改。勿删除本示例文件，便于日后对照。
 *
 * window.__WILSON_PUBLIC_CLOUD_SYNC__ = {
 *   firebaseConfig: { "apiKey": "...", "authDomain": "...", ... },
 *   syncDocId: "你的同步文档ID"
 * };
 */
window.__WILSON_PUBLIC_CLOUD_SYNC__ = {
    firebaseConfig: null,
    syncDocId: null
};
